const colors = require("colors/safe");
const mysql = require("mysql");
const prompt = require("prompt");
const fs = require("fs");

console.log(
  colors.blue(
    `All the module is ready! You can launch the program with "npm start" command.`
  )
);
